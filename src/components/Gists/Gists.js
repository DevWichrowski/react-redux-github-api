import React from 'react';
import './Gists.scss';
import {connect} from "react-redux";

const Gists = (props) => {
    return (
        <div>
            <h1>Gits</h1>

        </div>
    );
}

export default Gists;